#create-project

```
$ docker-compose up --build
$ docker exec -it node bash
```

#set up Next.js
```
(docker) $ npm init next-app project --example
(docker) $ cd project
(docker) $ npm run dev

```

